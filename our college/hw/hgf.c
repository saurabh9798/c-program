#include<stdio.h>
int main()
{
    char str[80] ,temp[20];
    int i,j;
    puts("Enter String :");
    gets(str);
    for(int i = 0;str[i] != '0'; i++)
    {
        if (str[i] != ' ')
        {
            temp[j++] = str[i];
            temp[j] = '\0';
            str.reverse
        }
        
    }
}

