//given an array containg element from 1 to 100 except one element in this range is missing . find the missing element
  #include<stdio.h>
  int main()
  {
    int sum = 0;
    for (int i = 0; i < 5; i++)
    {
      int i;
      //  sum = sum + int arr[i];
    }
    // int sum2 = 100*(100+1)/2;
    // int final = sum2 - sum;
     printf("the total sum is : %d",sum);
    return 0;
  }